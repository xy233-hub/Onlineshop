以下是统一格式后的接口文档，保持内容不变，统一了结构、描述方式及呈现逻辑：


### 通用说明
所有接口响应均采用统一封装格式：
```text
{
  "code": Number,        // 状态码（200 成功）
  "message": String,     // 提示信息
  "data": Object | Array | null  // 返回数据
}
```


### 1. 商品列表查询
- **方法**：GET
- **路径**：`/api/products`
- **描述**：查询商品列表，支持关键词、分类、状态、分页、排序等筛选（GET请求无请求体，参数通过查询字符串传递）
- **权限**：公开
- **查询参数**：
```text
{
  "q": String (可选),           // 关键词
  "category_id": Number (可选),
  "status": String (可选, "online"|"frozen"|"sold"|"outOfStock"),
  "page": Number (可选, 默认 1),
  "size": Number (可选, 默认 10),
  "sort_by": String (可选, 如 "price"|"created_at"),
  "order": String (可选, "asc"|"desc")
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,
  "items": [
    {
      "product_id": Number,
      "seller_id": Number,
      "category_id": Number,
      "product_name": String,
      "product_desc": String,            // 富媒体 HTML 或 Markdown
      "images": [ String ],              // 图片 URL 列表
      "price": Number,
      "stock_quantity": Number,
      "product_status": String,
      "search_keywords": String (可选),
      "created_at": String,
      "updated_at": String
    }
  ]
}
```


### 2. 单个商品详情查询
- **方法**：GET
- **路径**：`/api/products/{product_id}`
- **描述**：查询单个商品的详细信息（含图片、媒体、分类等）
- **权限**：公开
- **响应示例（data部分）**：
```text
{
  "product_id": Number,
  "seller_id": Number,
  "category_id": Number,
  "product_name": String,
  "product_desc": String,
  "images": [
    { "image_id": Number, "image_url": String, "image_order": Number }
  ],
  "media_resources": [
    { "media_id": Number, "media_type": String, "media_url": String, "file_name": String }
  ],
  "price": Number,
  "stock_quantity": Number,
  "product_status": String,
  "search_keywords": String (可选),
  "category": { "category_id": Number, "parent_id": Number | null, "category_name": String },
  "created_at": String,
  "updated_at": String
}
```


### 3. 提交购买意向（下单）
- **方法**：POST
- **路径**：`/api/products/purchase-intents`
- **描述**：登录客户提交购买意向，填写商品、数量、联系方式等
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "product_id": Number,
  "quantity": Number,                    // 可选，默认 1
  "customer_id": Number,                 // 必填，必须与Authorization中的用户一致
  "customer_name": String (可选),         // 可选，若不传使用客户注册信息
  "customer_phone": String (可选),        // 可选，若不传使用客户注册信息
  "customer_address": String (可选),     // 可选，若不传使用客户默认地址
  "note": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "purchase_id": Number,
  "product_id": Number,
  "customer_id": Number,
  "quantity": Number,
  "unit_price": Number,
  "total_amount": Number,
  "purchase_status": String,   // "pending"
  "contact_name": String,
  "contact_phone": String,
  "delivery_address": String,
  "seller_notes": String | null,
  "created_at": String,
  "updated_at": String
}
```


### 4. 客户注册
- **方法**：POST
- **路径**：`/api/customers/register`
- **描述**：客户注册账号，需填写用户名、密码、手机号等信息
- **权限**：公开
- **请求体（JSON）**：
```text
{
  "username": String,          // 6-20 位
  "password": String,
  "phone": String,
  "default_address": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "token": String,
  "customer_info": {
    "customer_id": Number,
    "username": String,
    "phone": String,
    "default_address": String | null,
    "created_at": String,
    "updated_at": String
  }
}
```


### 5. 客户登录
- **方法**：POST
- **路径**：`/api/customers/login`
- **描述**：客户登录，返回令牌（token）和客户信息
- **权限**：公开
- **请求体（JSON）**：
```text
{
  "username": String,
  "password": String
}
```
- **响应示例（data部分）**：
```text
{
  "token": String,
  "customer_info": {
    "customer_id": Number,
    "username": String,
    "phone": String,
    "default_address": String | null,
    "created_at": String,
    "updated_at": String
  }
}
```


### 6. 客户查询自己的购买意向记录
- **方法**：GET
- **路径**：`/api/customers/{customer_id}/purchase-intents`
- **描述**：查询客户自己的购买意向记录，支持分页和状态筛选
- **权限**：登录客户（需Authorization令牌，仅限本人查询）
- **查询参数**：
```text
{
  "page": Number (可选, 默认 1),
  "size": Number (可选, 默认 10),
  "purchase_status": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,
  "items": [
    {
      "purchase_id": Number,
      "product_id": Number,
      "quantity": Number,
      "total_amount": Number,
      "purchase_status": String,
      "seller_notes": String | null,
      "created_at": String,
      "updated_at": String
    }
  ]
}
```


### 7. 卖家登录
- **方法**：POST
- **路径**：`/api/seller/login`
- **描述**：卖家登录，返回令牌（token）和卖家信息
- **权限**：公开
- **请求体（JSON）**：
```text
{
  "username": String,
  "password": String
}
```
- **响应示例（data部分）**：
```text
{
  "token": String,
  "seller_info": {
    "seller_id": Number,
    "username": String,
    "create_time": String
  }
}
```


### 8. 卖家发布商品
- **方法**：POST
- **路径**：`/api/seller/products`
- **描述**：卖家发布商品，支持multipart/form-data或JSON格式（引用已上传URL）
- **权限**：卖家（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "product_name": "String (必填)",                // 与 products.product_name NOT NULL
  "product_desc": "String (必填)",                // 与 products.product_desc NOT NULL（富媒体 HTML/Markdown）
  "category_id": "Number (必填)",                 // 与 products.category_id NOT NULL，必须是存在的二级分类
  "price": "Number (必填, >=0)",                  // products.price NOT NULL
  "stock_quantity": "Number (可选, 默认 0)",       // products.stock_quantity NOT NULL DEFAULT 0
  "search_keywords": "String (可选)",             // products.search_keywords 可空
  "images": [                                     
    { "temp_key": "String"},
    {"image_url": "String ", "image_order": "Number (可选, 默认 0)" }
  ] (可选),                                       // 写入 product_images 表
  "media_resources": [
    { "temp_key": "String"},
    { "media_type": "String (可选, 'image'|'video'|'audio', 默认 'image')",
      "media_url": "String (必填)",
      "file_name": "String (必填)",
      "file_size": "Number (必填)",
      "mime_type": "String (必填)",
      "display_order": "Number (可选, 默认 0)",
      "is_embedded": "Boolean (可选, 默认 false)"
    }
  ] (可选)                                        // 写入 media_resources 表
}
```
- **响应示例（data部分）**：
```text
{
  "product_id": Number,
  "product_status": String,   // "online" 或 "outOfStock"
  "created_at": String,
  "updated_at": String
}
```


### 9. 卖家查询自己发布的商品列表
- **方法**：GET
- **路径**：`/api/seller/products`
- **描述**：卖家查询自己发布的商品列表，支持筛选和分页
- **权限**：卖家（需Authorization令牌）
- **查询参数**：
```text
{
  "status": String (可选),
  "page": Number (可选),
  "size": Number (可选)
}
```
- **响应示例（data部分）**：与通用商品列表响应格式一致（含历史记录）


### 10. 卖家冻结（下架）商品
- **方法**：PUT
- **路径**：`/api/seller/products/{product_id}/freeze`
- **描述**：卖家冻结（下架）某个商品，可填写原因
- **权限**：卖家（需Authorization令牌，仅限自己发布的商品）
- **请求体（JSON）**：
```text
{
  "reason": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "product_id": Number,
  "product_status": String,   // "frozen"
  "updated_at": String
}
```


### 11. 卖家解冻（重新上架）商品
- **方法**：PUT
- **路径**：`/api/seller/products/{product_id}/unfreeze`
- **描述**：卖家解冻（重新上架）商品，可填写备注
- **权限**：卖家（需Authorization令牌，仅限自己发布的商品）
- **请求体（JSON）**：
```text
{
  "remark": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "product_id": Number,
  "product_status": String,   // "online" 或 "outOfStock"（取决于库存）
  "updated_at": String
}
```


### 12. 卖家标记商品为已售出
- **方法**：PUT
- **路径**：`/api/seller/products/{product_id}/mark-sold`
- **描述**：卖家标记商品为已售出，更新库存和状态
- **权限**：卖家（需Authorization令牌，仅限自己发布的商品）
- **请求体（JSON）**：
```text
{
  "sold_quantity": Number (可选, 默认为 1),
  "note": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "product_id": Number,
  "product_status": String,   // 可能为 "sold" 或 "outOfStock"
  "stock_quantity": Number,
  "updated_at": String
}
```


### 13. 卖家查询收到的购买意向
- **方法**：GET
- **路径**：`/api/seller/purchase-intents`
- **描述**：卖家查询收到的所有购买意向，支持筛选和分页
- **权限**：卖家（需Authorization令牌）
- **查询参数**：
```text
{
  "purchase_status": String (可选),
  "product_id": Number (可选),
  "page": Number (可选),
  "size": Number (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,
  "items": [
    {
      "purchase_id": Number,
      "product_id": Number,
      "customer_id": Number | null,
      "customer_name": String,
      "customer_phone": String,
      "customer_address": String,
      "quantity": Number,
      "total_amount": Number,
      "purchase_status": String,
      "seller_notes": String | null,
      "created_at": String,
      "updated_at": String
    }
  ]
}
```


### 14. 卖家处理购买意向
- **方法**：PUT
- **路径**：`/api/seller/purchase-intents/{purchase_id}/status`
- **描述**：卖家处理购买意向，修改其状态（如成功、失败）并备注
- **权限**：卖家（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "new_status": String,   // "success" | "failed"
  "seller_notes": String (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "purchase_id": Number,
  "product_id": Number,
  "customer_id": Number | null,
  "quantity": Number,
  "total_amount": Number,
  "purchase_status": String,
  "seller_notes": String | null,
  "created_at": String,
  "updated_at": String
}
```


### 15. 卖家查询所有客户信息
- **方法**：GET
- **路径**：`/api/seller/customers`
- **描述**：卖家查询所有客户信息，支持分页，可带最近购买记录
- **权限**：卖家（需Authorization令牌）
- **查询参数**：
```text
{
  "page": Number (可选),
  "size": Number (可选)
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,
  "items": [
    {
      "customer_id": Number,
      "username": String,
      "phone": String,
      "default_address": String | null,
      "created_at": String,
      "updated_at": String,
      "recent_purchases": [
        { "purchase_id": Number, "product_id": Number, "total_amount": Number, "created_at": String }
      ] (可选)
    }
  ]
}
```


### 16. 卖家修改密码
- **方法**：PUT
- **路径**：`/api/seller/password`
- **描述**：卖家修改自己的登录密码
- **权限**：卖家（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "old_password": String,
  "new_password": String
}
```
- **响应示例（data部分）**：`null`（仅返回code和message）


### 17. 媒体资源上传
- **方法**：POST
- **路径**：`/api/media/upload`
- **描述**：上传媒体资源（图片、视频等），写入media_resources表以关联商品（需提供product_id）
- **权限**：卖家（需Authorization令牌）
- **请求体（multipart/form-data）**：
    - `file`：文件二进制（必填）
    - `purpose` (string)：可选，默认`gallery`。取值：
        - `gallery`：作为商品图片，插入到product_images
        - `embedded`：作为富媒体嵌入资源，插入到media_resources
- **响应示例（data部分）**：
```text
{
  "temp_key": "b3569547-a31e-4d98-8688-ef8089f29a88",
  "media_url": "http://localhost:8080/media/temp/b3569547-a31e-4d98-8688-ef8089f29a88.jpg",
  "file_size": 840749,
  "mime_type": "image/jpeg",
  "purpose": "embedded" 
}
```


### 18. 商品类别管理
#### 18.1 公开接口（用户/前端查询）
##### 18.1.1 获取类别列表（树形或扁平）
- **方法**：GET
- **路径**：`/api/categories`
- **描述**：供前端/用户查看类别树或扁平列表，支持筛选和分页
- **权限**：公开
- **查询参数**：
```text
{
  "tree": "true|false (可选, 默认 true)",
  "parent_id": Number (可选),
  "level": Number (可选, 1 或 2),
  "q": String (可选),
  "page": Number (可选, 默认 1),
  "size": Number (可选, 默认 50),
  "sort_by": "created_at|category_name" (可选),
  "order": "asc|desc" (可选)
}
```
- **响应示例**：tree=true时返回树形数组；tree=false时返回分页扁平结构


##### 18.1.2 查询单个类别详情
- **方法**：GET
- **路径**：`/api/categories/{category_id}`
- **描述**：查询单个类别的详细信息
- **权限**：公开
- **响应示例**：类别详情（含ID、名称、父ID、层级等）


#### 18.2 管理接口（卖家/有权限用户操作）
##### 18.2.1 获取分类列表（扁平）
- **方法**：GET
- **路径**：`/api/seller/categories`
- **描述**：获取分类的扁平列表（含分页）
- **权限**：卖家/管理员（需Authorization令牌）
- **查询参数**：与18.1.1一致
- **响应示例**：分页扁平结构（同18.1.1 tree=false时格式）


##### 18.2.2 获取分类树（层级）
- **方法**：GET
- **路径**：`/api/seller/categories/tree`
- **描述**：获取分类的树形层级结构
- **权限**：卖家/管理员（需Authorization令牌）
- **响应示例**：树形数组（同18.1.1 tree=true时格式）


##### 18.2.3 查询单个分类详情
- **方法**：GET
- **路径**：`/api/seller/categories/{category_id}`
- **描述**：查询单个分类的详细信息
- **权限**：卖家/管理员（需Authorization令牌）
- **响应示例**：分类详情（含ID、名称、父ID、层级等）


##### 18.2.4 新建分类
- **方法**：POST
- **路径**：`/api/seller/categories`
- **描述**：创建新分类（最大层级为2，商品上架需关联二级分类）
- **权限**：卖家/管理员（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "parent_id": Number | null,
  "category_name": "String"
}
```
- **响应示例（data部分）**：新建后的分类记录（含category_id、category_level、created_at等）


##### 18.2.5 更新分类
- **方法**：PUT
- **路径**：`/api/seller/categories/{category_id}`
- **描述**：更新分类信息
- **权限**：卖家/管理员（需Authorization令牌）
- **请求体（JSON）**：与18.2.4一致
- **响应示例（data部分）**：更新后的分类记录（含updated_at）


##### 18.2.6 删除分类
- **方法**：DELETE
- **路径**：`/api/seller/categories/{category_id}`
- **描述**：删除分类（仅当无子分类且无商品引用时允许）
- **权限**：卖家/管理员（需Authorization令牌）
- **响应示例（data部分）**：`null`（删除失败时message说明原因，如“存在子分类”或“存在商品引用”）


##### 18.2.7 移动/重设父分类或排序
- **方法**：PATCH
- **路径**：`/api/seller/categories/{category_id}/move`
- **描述**：调整分类的父分类或排序
- **权限**：卖家/管理员（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "new_parent_id": Number | null,
  "new_order": Number | null
}
```
- **响应示例（data部分）**：更新后的分类记录


### 19. 卖家查看顾客购买历史
- **方法**：GET
- **路径**：`/api/seller/customers/{customer_id}/purchase-history`
- **描述**：卖家查询指定顾客的购买历史记录
- **权限**：卖家（需Authorization令牌）
- **响应示例（data部分）**：
```text
{
  "purchase_history": [
    {
      "purchase_id": 5,
      "product_id": 4,
      "customer_id": 2,
      "customer_name": "xy233",
      "customer_phone": "12345678910",
      "customer_address": "zhejiang",
      "quantity": 1,
      "total_amount": 299.99,
      "purchase_status": "success",
      "seller_notes": null,
      "created_at": "2025-10-30T13:42:44",
      "updated_at": "2025-11-03T00:58:23"
    }
  ],
  "customer_info": {
    "customer_id": 2,
    "username": "xy2333",
    "phone": "12345678910",
    "default_address": "zhejiang",
    "created_at": "2025-10-30T12:56:45",
    "updated_at": "2025-10-30T12:56:45"
  }
}
```

#### 20. 客户添加商品到收藏夹（Favorite）
- **方法**：POST
- **路径**：`/api/customers/favorites`
- **描述**：客户收藏指定商品，同一客户对同一商品仅允许单次收藏（重复收藏返回成功提示，不重复创建）；购物车转收藏接口内部复用此接口的核心校验与创建逻辑
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "product_id": Number (必填)                   // 需为状态为"online"的商品
}
```
- **响应示例（data部分）**：
```text
{
  "favorite_id": Number,
  "customer_id": Number,
  "product_id": Number,
  "product_info": {
    "product_name": String,
    "product_desc": String,
    "price": Number,
    "image_url": String                         // 商品首图URL
  },
  "favorited_at": String
}
```


#### 21. 客户添加商品到购物车
- **方法**：POST
- **路径**：`/api/customers/cart/items`
- **描述**：支持单商品添加或批量添加商品到购物车，同一客户对同一商品重复添加时数量累加；批量添加时返回每个商品的添加结果（成功/失败原因）
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "product_id": Number (必填)  ,                 // 需为状态为"online"的商品
  "quantity": Number (必填) 
}
```
- **响应示例（data部分）**：
```text
{
   "quantity": 4,
   "cart_item_id": 18
}
```


### 22. 客户从购物车选择多件商品下单
- **方法**：POST
- **路径**：`/api/customers/cart/batch-purchase`
- **描述**：客户选择购物车中多个商品统一下单，下单成功后对应购物车项自动删除；支持部分商品下单失败时返回失败原因（如库存不足）
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "cart_item_ids": [Number] (必填),              // 购物车项ID数组（至少1个）
  "contact_name": String (可选),                 // 可选，若不传使用客户注册姓名
  "contact_phone": String (可选),                // 可选，若不传使用客户注册手机号
  "delivery_address": String (可选),             // 可选，若不传使用客户默认地址
  "note": String (可选)                          // 订单备注
}
```
- **响应示例（data部分）**：
```text
{
  "order_id": Number,
  "customer_id": Number,
  "total_amount": Number,
  "order_status": String,                       // "pending"（待处理）
  "contact_name": String,
  "contact_phone": String,
  "delivery_address": String,
  "note": String | null,
  "items": [
    {
      "product_id": Number,
      "product_name": String,
      "quantity": Number,
      "unit_price": Number,
      "subtotal": Number
    }
  ],
  "failed_items": [                             // 下单失败的商品（无失败则为空数组）
    {
      "cart_item_id": Number,
      "product_id": Number,
      "product_name": String,
      "reason": String                          // 失败原因（如"商品已下架"）
    }
  ],
  "created_at": String,
  "updated_at": String
}
```


#### 23. 客户从购物车选择多件商品转为收藏（Favorite）
- **方法**：POST
- **路径**：`/api/customers/cart/batch-convert-favorite`
- **描述**：客户将购物车中多个商品转为收藏，内部复用“添加收藏”接口的核心逻辑；转换成功后对应购物车项自动删除；已收藏的商品仍可转换（不重复创建，仅删除购物车项）
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "cart_item_ids": [Number] (必填)               // 购物车项ID数组（至少1个）
}
```
- **响应示例（data部分）**：
```text
{
  "converted_count": Number,                    // 成功转换的数量
  "failed_count": Number,                       // 转换失败的数量
  "converted_favorites": [
    {
      "favorite_id": Number,
      "product_id": Number,
      "product_name": String,
      "favorited_at": String
    }
  ],
  "failed_items": [                             // 转换失败的购物车项（无失败则为空数组）
    {
      "cart_item_id": Number,
      "product_id": Number,
      "product_name": String,
      "reason": String                          // 失败原因（如"商品已下架"）
    }
  ]
}
```


#### 24. 客户查询收藏列表（Favorite）（保持不变）
- **方法**：GET
- **路径**：`/api/customers/favorites`
- **描述**：客户查询自己的所有收藏商品，支持分页筛选
- **权限**：登录客户（需Authorization令牌）
- **查询参数**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "page": Number (可选, 默认 1),
  "size": Number (可选, 默认 10),
  "sort_by": String (可选, 如 "favorited_at"),
  "order": String (可选, "asc"|"desc")
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,
  "items": [
    {
      "favorite_id": Number,
      "product_id": Number,
      "product_info": {
        "product_name": String,
        "product_desc": String,
        "price": Number,
        "image_url": String,
        "stock_quantity": Number,
        "product_status": String
      },
      "created_at": String
    }
  ]
}
```

#### 25. 客户取消收藏（Favorite）（保持不变）
- **方法**：DELETE
- **路径**：`/api/customers/favorites/{favorite_id}`
- **描述**：客户取消对指定商品的收藏，删除对应收藏记录
- **权限**：登录客户（需Authorization令牌，仅限本人收藏记录）
- **响应示例（data部分）**：
```text
{
  "favorite_id": Number,
  "customer_id": Number,
  "message": "取消收藏成功",
  "updated_at": String
}
```

完全正确！购物车功能需要配套的“查询购物车列表”和“删除购物车项”接口才能形成完整闭环，下面补充这两个核心接口，严格遵循之前的统一格式规范，与现有接口逻辑保持一致：

### 26. 客户查询购物车列表
- **方法**：GET
- **路径**：`/api/customers/cart/items`
- **描述**：客户查询自己的购物车所有商品，支持分页、筛选（如仅显示库存充足商品），返回商品当前状态（确保下单前信息准确）
- **权限**：登录客户（需Authorization令牌）
- **查询参数**：
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "page": Number (可选, 默认 1),
  "size": Number (可选, 默认 10),
  "filter_stock": Boolean (可选, 默认 false),   // 是否仅显示库存充足商品（true=仅显示，false=全部显示）
  "sort_by": String (可选, 如 "created_at"|"price"),
  "order": String (可选, "asc"|"desc")
}
```
- **响应示例（data部分）**：
```text
{
  "page": Number,
  "size": Number,
  "total": Number,                              // 购物车商品总数量
  "total_amount": Number,                       // 购物车所有商品总价（按当前单价计算）
  "items": [
    {
      "cart_item_id": Number,
      "product_id": Number,
      "product_info": {
        "product_name": String,
        "product_desc": String,
        "price": Number,                        // 商品当前单价（可能与加入时不同）
        "stock_quantity": Number,               // 商品当前库存
        "image_url": String,
        "product_status": String                // 商品当前状态（online/outOfStock/frozen/sold）
      },
      "quantity": Number,
      "unit_price": Number,                     // 商品加入购物车时的单价（参考用）
      "subtotal": Number,                       // 该商品小计（当前单价 × 数量）
      "note": String | null,
      "created_at": String,
      "updated_at": String,
      "stock_enough": Boolean                   // 是否库存充足（方便前端提示）
    }
  ]
}
```


### 27. 客户删除购物车项（支持单个/批量）
- **方法**：DELETE
- **路径**：`/api/customers/cart/items`
- **描述**：客户删除购物车中单个或多个商品，支持批量操作，删除前校验购物车项是否存在（仅能删除本人的购物车项）
- **权限**：登录客户（需Authorization令牌）
- **请求体（JSON）**：（注：DELETE方法支持请求体，兼容多端调用；若部分端不支持，可改为查询参数，但请求体更适合传递数组）
```text
{
  "customer_id": Number (必填),                 // 必须与Authorization中的用户一致
  "cart_item_ids": [Number] (必填)               // 购物车项ID数组（至少1个，支持单个/多个）
}
```
- **单个删除请求示例**：
```text
{
  "customer_id": 1001,
  "cart_item_ids": [3001]
}
```
- **批量删除请求示例**：
```text
{
  "customer_id": 1001,
  "cart_item_ids": [3001, 3002, 3003]
}
```
- **响应示例（data部分）**：
```text
{
  "deleted_count": Number,                      // 成功删除的购物车项数量
  "failed_count": Number,                       // 删除失败的数量
  "deleted_cart_item_ids": [Number],            // 成功删除的购物车项ID
  "failed_items": [                             // 删除失败的项（无失败则为空数组）
    {
      "cart_item_id": Number,
      "reason": String                          // 失败原因（如"购物车项不存在"、"无权限删除"）
    }
  ]
}
```
### 28： 客户取消订单

- 方法： POST

- 路径：`/api/customers/{customer_id}/purchase-intents/{purchase_id}/cancel`

- 权限：客户（需 `Authorization: Bearer <token>`）

  ```json
  {
    "code": 200,
    "message": "订单取消成功",
    "data": {
      "purchase_id": 5,
      "product_id": 4,
      "customer_id": 2,
      "quantity": 1,
      "total_amount": 299.99,
      "purchase_status": "CUSTOMER_CANCELLED",
      "cancel_reason": "不想要了",
      "cancel_notes": "已找到更合适的商品",
      "created_at": "2025-10-30T13:42:44",
      "updated_at": "2025-11-03T00:58:23"
    }
  }
  ```

  请求体示例：

  ```json
    {
      "cancel_reason": "不想要了",
      "cancel_notes": "已找到更合适的商品"
    }
  ```

### 29： 客户确认收货

- 方法：POST

- 路径：`/api/customers/{customer_id}/purchase-intents/{purchase_id}/confirm-received`

- 权限：客户（需 `Authorization: Bearer <token>`）

- 请求体：无

  ```json
  {
    "code": 200,
    "message": "确认收货成功",
    "data": {
      "purchase_id": 5,
      "product_id": 4,
      "customer_id": 2,
      "quantity": 1,
      "total_amount": 299.99,
      "purchase_status": "COMPLETED",
      "created_at": "2025-10-30T13:42:44",
      "updated_at": "2025-11-03T00:58:23"
    }
  }
  ```

### 统一错误响应格式
```text
{ "code": 400, "message": "错误原因说明（如：库存不足）", "data": null }
```



### 30. 创建支付记录（客户下单后）

- **方法**：POST

- **路径**：`/api/payments/create`

- **描述**：客户提交订单后，系统自动生成支付记录，设置支付过期时间为订单创建后 30 分钟；同一订单仅允许存在一个有效支付记录

- **权限**：登录客户（需Authorization令牌）

- **请求体（JSON）**：

  ```json
  {
    "purchaseId": Number (必填),                 // 购买意向 ID（订单 ID）
    "amount": Number (必填)                      // 支付金额（必须与订单总金额一致）
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "支付记录创建成功",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "customer_id": 101,
      "payment_method": null,
      "payment_amount": 299.99,
      "payment_status": "PENDING",
      "payment_expiry": "2026-03-04T16:00:00",
      "payment_notes": "订单创建后自动生成，待支付",
      "created_at": "2026-03-04T15:30:00",
      "updated_at": "2026-03-04T15:30:00"
    }
  }
  ```

  

### 31. 选择支付方式并发起支付

- **方法**：POST

- **路径**：`/api/payments/{paymentId}/pay`

- **描述**：客户选择支付方式（银行卡、信用卡、支付宝、微信支付）并发起支付请求；系统记录支付方式后跳转至第三方支付页面

- **权限**：登录客户（需Authorization令牌）

- **路径参数**：

  ```
  paymentId: Number                              // 支付记录 ID
  ```

- **请求体（JSON）**：

  ```json
  {
    "paymentMethod": String (必填)               // 支付方式："BANK_CARD"|"CREDIT_CARD"|"ALIPAY"|"WECHAT_PAY"
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "支付请求已受理",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "payment_method": "ALIPAY",
      "payment_status": "PENDING",
      "pay_url": "https://openapi.alipay.com/gateway.do?trade_no=xxx",
      "qr_code": "https://qr.alipay.com/xxx",
      "transaction_id": null
    }
  }
  ```

  

### 33. 支付失败回调（第三方支付平台异步通知）

- **方法**：`POST`

- **路径**：`/api/payments/{paymentId}/failure`

- **描述**：支付失败时由第三方支付平台异步调用（用户主动取消支付或支付失败），记录失败原因，订单状态保持“待支付”。

- **权限**：公开（供第三方回调调用）

- **路径参数**：

  - `paymentId`: `1001` (Number, 必填) - 支付记录 ID

- **请求体（JSON）**：

  json

  ```
  {
    "reason": "用户取消支付"   // String (必填), 失败原因（如“余额不足”、“用户取消”）
  }
  ```

  

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "支付失败已记录",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "payment_status": "FAILED",
      "payment_notes": "用户主动取消支付",
      "updated_at": "2026-03-04T15:40:00"
    }
  }
  ```

### 34. 手动触发支付结果校验

- **方法**：`POST`

- **路径**：`/api/payments/verify`

- **描述**：当支付结果回调失败或网络异常时，客户可手动触发支付结果校验；系统调用第三方支付接口查询实际支付状态并更新本地记录。

- **权限**：登录客户（需 Authorization 令牌，仅限本人订单）

- **请求体（JSON）**：

  json

  ```
  {
    "purchaseId": 5001   // Number (必填), 购买意向 ID（订单 ID）
  }
  ```

  

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "支付状态查询成功",
    "data": {
      "payment_status": "PAID",
      "payment_method": "ALIPAY",
      "transaction_id": "2026030422001001",
      "payment_time": "2026-03-04T15:35:00",
      "verified_at": "2026-03-04T15:45:00"
    }
  }
  ```

  

### 35. 查询客户支付记录列表

- **方法**：`GET`

- **路径**：`/api/payments/customer`

- **描述**：客户查询自己的所有支付记录，支持按支付状态、支付方式筛选。

- **权限**：登录客户（需 Authorization 令牌）

- **查询参数**：

  - `page`: `1` (Number, 可选, 默认 1)
  - `size`: `10` (Number, 可选, 默认 10)
  - `paymentStatus`: `PAID` (String, 可选) - `"PENDING"|"PAID"|"FAILED"|"REFUNDING"|"REFUNDED"`
  - `paymentMethod`: `ALIPAY` (String, 可选) - `"BANK_CARD"|"CREDIT_CARD"|"ALIPAY"|"WECHAT_PAY"`

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 5,
      "items": [
        {
          "payment_id": 1001,
          "purchase_id": 5001,
          "order_info": {
            "product_name": "iPhone 15 Pro",
            "total_amount": 7999.00
          },
          "payment_method": "ALIPAY",
          "payment_amount": 7999.00,
          "payment_status": "PAID",
          "transaction_id": "2026030422001001",
          "payment_time": "2026-03-04T15:35:00",
          "payment_expiry": "2026-03-04T16:00:00",
          "created_at": "2026-03-04T15:30:00"
        }
      ]
    }
  }
  ```

  

### 36. 根据订单 ID 查询支付记录

- **方法**：`GET`

- **路径**：`/api/payments/purchase/{purchaseId}`

- **描述**：根据购买意向 ID（订单 ID）查询对应的支付记录详情。

- **权限**：登录客户（需 Authorization 令牌，仅限本人订单）

- **路径参数**：

  - `purchaseId`: `5001` (Number, 必填) - 购买意向 ID

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "customer_id": 101,
      "payment_method": "ALIPAY",
      "payment_amount": 299.99,
      "payment_status": "PAID",
      "transaction_id": "2026030422001001",
      "payment_time": "2026-03-04T15:35:00",
      "refund_time": null,
      "refund_amount": null,
      "refund_reason": null,
      "payment_expiry": "2026-03-04T16:00:00",
      "payment_notes": "支付成功",
      "created_at": "2026-03-04T15:30:00",
      "updated_at": "2026-03-04T15:35:00"
    }
  }
  ```

  

### 37. 申请退款

- **方法**：`POST`

- **路径**：`/api/payments/{paymentId}/refund/apply`

- **描述**：客户对已支付的订单申请退款，填写退款原因和金额；退款申请提交后订单支付状态变更为“退款中”。

- **权限**：登录客户（需 Authorization 令牌，仅限本人订单）

- **路径参数**：

  - `paymentId`: `1001` (Number, 必填) - 支付记录 ID

- **请求体（JSON）**：

  json

  ```
  {
    "refundAmount": 299.99,    // Number (必填), 退款金额（必须 <= 支付金额）
    "refundReason": "商品有质量问题" // String (必填), 退款原因
  }
  ```

  

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "退款申请已提交",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "payment_status": "REFUNDING",
      "refund_amount": 299.99,
      "refund_reason": "商品有质量问题",
      "applied_at": "2026-03-05T10:00:00"
    }
  }
  ```

  

### 38. 卖家处理退款申请

- **方法**：`POST`

- **路径**：`/api/seller/payments/{paymentId}/refund`

- **描述**：卖家审核并处理客户的退款申请。若同意退款，系统自动将资金原路退回，并向客户发送站内提醒。

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - `paymentId`: `1001` (Number, 必填) - 支付记录 ID

- **请求体（JSON）**：

  json

  ```
  {
    "refundAmount": 299.99,    // Number (必填), 退款金额
    "refundReason": "同意退款：商品确有质量问题", // String (必填), 退款原因（卖家备注）
    "approved": true            // Boolean (必填), true=同意退款，false=拒绝退款
  }
  ```

  

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "退款处理成功",
    "data": {
      "payment_id": 1001,
      "purchase_id": 5001,
      "payment_status": "REFUNDED",
      "refund_amount": 299.99,
      "refund_reason": "同意退款：商品确有质量问题",
      "refund_time": "2026-03-05T14:30:00",
      "updated_at": "2026-03-05T14:30:00"
    }
  }
  ```

  

### 39. 查询即将过期的支付记录

- **方法**：`GET`

- **路径**：`/api/seller/payments/expiring`

- **描述**：查询即将过期（剩余时间 < 指定分钟数）的待支付订单，供卖家查看或系统定时任务调用以发送提醒。

- **权限**：卖家（需 Authorization 令牌）

- **查询参数**：

  - `minutes`: `5` (Number, 可选, 默认 5) - 查询剩余多少分钟内过期的订单

- **响应示例**：

  json

  ```
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "expiring_count": 2,
      "items": [
        {
          "payment_id": 1002,
          "purchase_id": 5002,
          "customer_info": {
            "customer_id": 102,
            "username": "zhangsan",
            "phone": "138****1234"
          },
          "order_info": {
            "product_name": "MacBook Pro 14",
            "total_amount": 12999.00
          },
          "payment_amount": 12999.00,
          "payment_expiry": "2026-03-04T16:03:00",
          "remaining_minutes": 3
        }
      ]
    }
  }
  ```

  

### 40. 支付过期自动关闭（系统定时任务）

- **方法**：`无`（系统内部定时任务，建议每 5 分钟执行一次）
- **路径**：`无`
- **描述**：系统定时检查支付过期时间，将超过 30 分钟未支付的订单自动标记为“支付失败”，同时释放锁定的库存，并向客户发送站内提醒。
- **权限**：系统自动执行
- **触发逻辑**：
  1. 查询 `payments` 表中 `payment_status='PENDING'` 且 `payment_expiry < 当前时间` 的记录。
  2. 批量更新这些记录的 `payment_status='FAILED'`。
  3. 更新关联订单 `purchase_intents` 的支付状态为 `"UNPAID"`。
  4. 释放下单时锁定的库存。
  5. 发送站内消息提醒客户支付已超时。
- **日志记录**：每次执行后记录处理的订单数量到系统日志。

### 41. 客户添加收货地址

- **方法**：POST

- **路径**：`/api/customers/addresses`

- **描述**：客户添加新的收货地址，支持设置是否为默认地址（若设置为默认，系统自动取消其他地址的默认状态）

- **权限**：登录客户（需 Authorization 令牌）

- **请求体（JSON）**：

  ```json
  {
    "customer_id": Number (必填),                 // 必须与 Authorization 中的用户一致
    "recipient_name": String (必填),              // 收货人姓名
    "recipient_phone": String (必填),             // 收货人联系电话
    "province": String (必填),                    // 省份/直辖市
    "city": String (必填),                        // 城市
    "district": String (必填),                    // 区/县
    "detail_address": String (必填),              // 详细地址（街道、门牌号等）
    "is_default": Boolean (可选，默认 false)       // 是否设为默认地址
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "地址添加成功",
    "data": {
      "address_id": 1001,
      "customer_id": 101,
      "recipient_name": "张三",
      "recipient_phone": "138****1234",
      "province": "浙江省",
      "city": "杭州市",
      "district": "西湖区",
      "detail_address": "文三路 100 号",
      "is_default": true,
      "created_at": "2026-03-05T10:00:00",
      "updated_at": "2026-03-05T10:00:00"
    }
  }
  ```

  

### 42. 客户编辑收货地址

- **方法**：PUT

- **路径**：`/api/customers/addresses/{address_id}`

- **描述**：客户编辑已保存的收货地址信息，可修改所有字段

- **权限**：登录客户（需 Authorization 令牌，仅限本人地址）

- **路径参数**：

  - address_id: 1001 (Number, 必填) - 地址 ID

  

- **请求体（JSON）**：

  ```json
  {
    "recipient_name": String (必填),
    "recipient_phone": String (必填),
    "province": String (必填),
    "city": String (必填),
    "district": String (必填),
    "detail_address": String (必填),
    "is_default": Boolean (可选)
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "地址更新成功",
    "data": {
      "address_id": 1001,
      "customer_id": 101,
      "recipient_name": "张三",
      "recipient_phone": "139****5678",
      "province": "浙江省",
      "city": "杭州市",
      "district": "余杭区",
      "detail_address": "文一西路 200 号",
      "is_default": true,
      "updated_at": "2026-03-05T11:00:00"
    }
  }
  ```

  

### 43. 客户删除收货地址

- **方法**：DELETE

- **路径**：`/api/customers/addresses/{address_id}`

- **描述**：客户删除指定的收货地址；若删除的是默认地址，系统自动将第一个地址设为默认

- **权限**：登录客户（需 Authorization 令牌，仅限本人地址）

- **路径参数**：

  - address_id: 1001 (Number, 必填) - 地址 ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "地址删除成功",
    "data": {
      "deleted_address_id": 1001,
      "new_default_address_id": 1002  // 若产生了新的默认地址则返回，否则不返回此字段
    }
  }
  ```

  

### 44. 客户查询收货地址列表

- **方法**：GET

- **路径**：`/api/customers/addresses`

- **描述**：客户查询自己的所有收货地址，默认地址排在首位

- **权限**：登录客户（需 Authorization 令牌）

- **查询参数**：

  - customer_id: 101 (Number, 必填) - 必须与 Authorization 中的用户一致

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": [
      {
        "address_id": 1001,
        "customer_id": 101,
        "recipient_name": "张三",
        "recipient_phone": "138****1234",
        "province": "浙江省",
        "city": "杭州市",
        "district": "西湖区",
        "detail_address": "文三路 100 号",
        "is_default": true,
        "created_at": "2026-03-05T10:00:00"
      },
      {
        "address_id": 1002,
        "customer_id": 101,
        "recipient_name": "李四",
        "recipient_phone": "139****5678",
        "province": "浙江省",
        "city": "宁波市",
        "district": "鄞州区",
        "detail_address": "天童南路 500 号",
        "is_default": false,
        "created_at": "2026-03-05T10:30:00"
      }
    ]
  }
  ```

  

### 45. 客户设置默认收货地址

- **方法**：PATCH

- **路径**：`/api/customers/addresses/{address_id}/default`

- **描述**：客户将指定地址设为默认地址，系统自动取消其他地址的默认状态

- **权限**：登录客户（需 Authorization 令牌，仅限本人地址）

- **路径参数**：

  - address_id: 1001 (Number, 必填) - 地址 ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "默认地址设置成功",
    "data": {
      "address_id": 1001,
      "is_default": true,
      "updated_at": "2026-03-05T12:00:00"
    }
  }
  ```

  

### 46. 获取物流商列表

- **方法**：GET

- **路径**：`/api/logistics/providers`

- **描述**：获取所有启用的物流服务商列表，供下单时选择

- **权限**：公开

- **查询参数**：

  - type: EXPRESS (String, 可选) - 物流类型："EXPRESS"= 快递，"OFFLINE"= 线下配送，不传则返回全部

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": [
      {
        "provider_id": 1,
        "provider_name": "顺丰速运",
        "provider_code": "SF",
        "enabled": true,
        "sort_order": 1
      },
      {
        "provider_id": 2,
        "provider_name": "京东物流",
        "provider_code": "JD",
        "enabled": true,
        "sort_order": 2
      },
      {
        "provider_id": 8,
        "provider_name": "线下配送",
        "provider_code": "OFFLINE",
        "enabled": true,
        "sort_order": 8
      }
    ]
  }
  ```

  

### 47. 卖家发货（填写物流信息）

- **方法**：POST

- **路径**：`/api/seller/purchase-intents/{purchase_id}/ship`

- **描述**：卖家对订单进行发货操作，填写物流公司和运单号；系统自动更新订单状态为 "SHIPPING_STARTED" 并记录发货时间

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - purchase_id: 5001 (Number, 必填) - 购买意向 ID

- **请求体（JSON）**：

  ```json
  {
    "logistics_provider_id": Number (必填),     // 物流商 ID
    "tracking_no": String (必填)                // 物流单号
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "发货成功",
    "data": {
      "purchase_id": 5001,
      "purchase_status": "SHIPPING_STARTED",
      "logistics_provider_id": 1,
      "logistics_provider_name": "顺丰速运",
      "tracking_no": "SF1234567890",
      "shipped_at": "2026-03-05T14:00:00",
      "updated_at": "2026-03-05T14:00:00"
    }
  }
  ```

  

### 48. 查询订单物流轨迹

- **方法**：GET

- **路径**：`/api/orders/{purchase_id}/logistics`

- **描述**：查询订单的物流配送信息及完整物流轨迹；仅当订单状态为 "SHIPPING_STARTED"、"COMPLETED" 时可查询

- **权限**：登录客户（需 Authorization 令牌，仅限本人订单）

- **路径参数**：

  - purchase_id: 5001 (Number, 必填) - 购买意向 ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "purchase_id": 5001,
      "purchase_status": "SHIPPING_STARTED",
      "logistics_info": {
        "provider_id": 1,
        "provider_name": "顺丰速运",
        "provider_code": "SF",
        "tracking_no": "SF1234567890",
        "shipped_at": "2026-03-05T14:00:00"
      },
      "current_status": "运输中",
      "tracks": [
        {
          "track_id": 1,
          "track_time": "2026-03-05T14:30:00",
          "track_content": "快件已从杭州集散中心发出，下一站北京",
          "track_location": "杭州市",
          "track_status": "运输中"
        },
        {
          "track_id": 2,
          "track_time": "2026-03-05T18:00:00",
          "track_content": "快件已到达北京集散中心",
          "track_location": "北京市",
          "track_status": "运输中"
        },
        {
          "track_id": 3,
          "track_time": "2026-03-06T09:00:00",
          "track_content": "快件已由快递员张三收件，准备派送",
          "track_location": "北京市朝阳区",
          "track_status": "派送中"
        }
      ]
    }
  }
  ```

  

### 49. 卖家手动添加物流轨迹

- **方法**：POST

- **路径**：`/api/seller/orders/{purchase_id}/logistics/tracks`

- **描述**：卖家手动添加物流轨迹记录（适用于无第三方物流接口的场景），系统自动追加到轨迹列表末尾

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - purchase_id: 5001 (Number, 必填) - 购买意向 ID

- **请求体（JSON）**：

  ```json
  {
    "track_content": String (必填),             // 轨迹内容描述
    "track_location": String (可选),            // 轨迹发生地点
    "track_status": String (可选)               // 当前物流状态
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "物流轨迹添加成功",
    "data": {
      "track_id": 1001,
      "purchase_id": 5001,
      "track_time": "2026-03-06T10:00:00",
      "track_content": "买家已确认收货",
      "track_location": null,
      "track_status": "已签收"
    }
  }
  ```

  

### 50. 客户发起售后服务申请

- **方法**：POST

- **路径**：`/api/customers/after-sales`

- **描述**：客户针对已购买的订单发起售后服务申请，需填写售后类型、标题、问题描述及上传凭证图片

- **权限**：登录客户（需 Authorization 令牌）

- **请求体（JSON）**：

  ```json
  {
    "purchase_id": Number (必填),               // 购买意向 ID
    "product_id": Number (必填),                // 商品 ID
    "service_type": String (必填),              // "REFUND_ONLY"=仅退款，"RETURN_AND_REFUND"=退货退款，"EXCHANGE"=换货
    "service_title": String (必填),             // 售后标题（100 字内）
    "problem_description": String (必填),       // 问题描述（富文本 HTML）
    "evidence_images": [String] (可选),         // 凭证图片 URL 数组
    "refund_amount": Number (必填)              // 申请退款金额（必须 <= 订单实付金额）
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "售后申请已提交",
    "data": {
      "service_id": 2001,
      "purchase_id": 5001,
      "product_id": 101,
      "customer_id": 101,
      "service_type": "REFUND_ONLY",
      "service_title": "商品质量问题申请退款",
      "problem_description": "收到商品后发现存在明显划痕...",
      "evidence_images": [
        "http://localhost:8080/media/evidence/img1.jpg",
        "http://localhost:8080/media/evidence/img2.jpg"
      ],
      "refund_amount": 299.99,
      "service_status": "PENDING",
      "created_at": "2026-03-06T10:00:00",
      "updated_at": "2026-03-06T10:00:00"
    }
  }
  ```

  

### 51. 客户查询售后服务列表

- **方法**：GET

- **路径**：`/api/customers/after-sales`

- **描述**：客户查询自己的所有售后服务申请记录，支持按状态筛选和分页

- **权限**：登录客户（需 Authorization 令牌）

- **查询参数**：

  - customer_id: 101 (Number, 必填) - 必须与 Authorization 中的用户一致
  - page: 1 (Number, 可选，默认 1)
  - size: 10 (Number, 可选，默认 10)
  - service_status: PENDING (String, 可选) - "PENDING"|"NEGOTIATING"|"AGREED"|"REJECTED"|"COMPLETED"|"CANCELLED"

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 3,
      "items": [
        {
          "service_id": 2001,
          "purchase_id": 5001,
          "product_info": {
            "product_id": 101,
            "product_name": "无线蓝牙耳机",
            "product_image": "http://localhost:8080/media/product/img1.jpg"
          },
          "service_type": "REFUND_ONLY",
          "service_title": "商品质量问题申请退款",
          "refund_amount": 299.99,
          "service_status": "NEGOTIATING",
          "seller_response": "亲，可以给您换货哦~",
          "created_at": "2026-03-06T10:00:00",
          "updated_at": "2026-03-06T14:00:00"
        }
      ]
    }
  }
  ```

  

### 52. 客户查询售后服务详情

- **方法**：GET

- **路径**：`/api/customers/after-sales/{service_id}`

- **描述**：客户查询指定售后服务单的详细信息，包括处理进度、商家回复等

- **权限**：登录客户（需 Authorization 令牌，仅限本人申请）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "service_id": 2001,
      "purchase_id": 5001,
      "order_info": {
        "product_name": "无线蓝牙耳机",
        "order_amount": 299.99,
        "order_status": "COMPLETED"
      },
      "service_type": "REFUND_ONLY",
      "service_title": "商品质量问题申请退款",
      "problem_description": "收到商品后发现存在明显划痕，影响使用体验...",
      "evidence_images": [
        "http://localhost:8080/media/evidence/img1.jpg",
        "http://localhost:8080/media/evidence/img2.jpg"
      ],
      "refund_amount": 299.99,
      "service_status": "NEGOTIATING",
      "seller_response": "亲，非常抱歉给您带来不好的体验。可以给您换货或者全额退款，您看怎么处理比较好呢？",
      "seller_decision": null,
      "seller_decision_at": null,
      "created_at": "2026-03-06T10:00:00",
      "updated_at": "2026-03-06T14:00:00"
    }
  }
  ```

  

### 53. 客户取消售后服务申请

- **方法**：POST

- **路径**：`/api/customers/after-sales/{service_id}/cancel`

- **描述**：客户取消已提交的售后服务申请（仅限状态为 "PENDING" 或 "NEGOTIATING" 的申请）

- **权限**：登录客户（需 Authorization 令牌，仅限本人申请）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **请求体（JSON）**：

  ```json
  {
    "cancel_reason": String (必填)              // 取消原因
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "售后申请已取消",
    "data": {
      "service_id": 2001,
      "service_status": "CANCELLED",
      "cancel_reason": "与商家协商一致，不需要售后了",
      "cancelled_at": "2026-03-06T15:00:00",
      "updated_at": "2026-03-06T15:00:00"
    }
  }
  ```

  

### 54. 客户退货发货（退货退款流程）

- **方法**：POST

- **路径**：`/api/customers/after-sales/{service_id}/return-ship`

- **描述**：在退货退款流程中，客户填写退货物流信息并确认发货；系统更新售后状态为等待商家收货

- **权限**：登录客户（需 Authorization 令牌，仅限本人申请）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **请求体（JSON）**：

  ```json
  {
    "return_tracking_no": String (必填),        // 退货物流单号
    "return_logistics_provider": String (必填)  // 退货物流公司名称
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "退货发货信息已提交",
    "data": {
      "service_id": 2001,
      "service_status": "NEGOTIATING",
      "return_tracking_no": "YT123456789",
      "return_logistics_provider": "圆通速递",
      "return_shipped_at": "2026-03-07T10:00:00",
      "updated_at": "2026-03-07T10:00:00"
    }
  }
  ```

  

### 55. 卖家查询售后服务列表

- **方法**：GET

- **路径**：`/api/seller/after-sales`

- **描述**：卖家查询收到的所有售后服务申请，支持按状态筛选、分页，供商家处理售后问题

- **权限**：卖家（需 Authorization 令牌）

- **查询参数**：

  - page: 1 (Number, 可选，默认 1)
  - size: 10 (Number, 可选，默认 10)
  - service_status: PENDING (String, 可选) - "PENDING"|"NEGOTIATING"|"AGREED"|"REJECTED"|"COMPLETED"
  - service_type: REFUND_ONLY (String, 可选) - "REFUND_ONLY"|"RETURN_AND_REFUND"|"EXCHANGE"

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 5,
      "items": [
        {
          "service_id": 2001,
          "purchase_id": 5001,
          "customer_info": {
            "customer_id": 101,
            "username": "zhangsan",
            "phone": "138****1234"
          },
          "product_info": {
            "product_id": 101,
            "product_name": "无线蓝牙耳机"
          },
          "service_type": "REFUND_ONLY",
          "service_title": "商品质量问题申请退款",
          "refund_amount": 299.99,
          "service_status": "PENDING",
          "created_at": "2026-03-06T10:00:00"
        }
      ]
    }
  }
  ```

  

### 56. 卖家查询售后服务详情

- **方法**：GET

- **路径**：`/api/seller/after-sales/{service_id}`

- **描述**：卖家查看指定售后服务单的完整信息，包括客户提交的所有凭证和描述

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "service_id": 2001,
      "purchase_id": 5001,
      "customer_info": {
        "customer_id": 101,
        "username": "zhangsan",
        "phone": "138****1234"
      },
      "order_info": {
        "product_id": 101,
        "product_name": "无线蓝牙耳机",
        "order_amount": 299.99,
        "order_status": "COMPLETED"
      },
      "service_type": "REFUND_ONLY",
      "service_title": "商品质量问题申请退款",
      "problem_description": "收到商品后发现存在明显划痕，影响使用体验...",
      "evidence_images": [
        "http://localhost:8080/media/evidence/img1.jpg",
        "http://localhost:8080/media/evidence/img2.jpg"
      ],
      "refund_amount": 299.99,
      "service_status": "PENDING",
      "seller_response": null,
      "seller_decision": null,
      "created_at": "2026-03-06T10:00:00",
      "updated_at": "2026-03-06T10:00:00"
    }
  }
  ```

  

### 57. 卖家处理售后服务申请

- **方法**：POST

- **路径**：`/api/seller/after-sales/{service_id}/handle`

- **描述**：卖家审核并处理客户的售后申请，可选择同意退款、同意退货退款、同意换货或拒绝售后；处理后系统自动更新售后状态并通知客户

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **请求体（JSON）**：

  ```json
  {
    "seller_decision": String (必填),           // "AGREE_REFUND"=同意退款，"AGREE_RETURN_REFUND"=同意退货退款，"AGREE_EXCHANGE"=同意换货，"REJECT"=拒绝售后
    "seller_response": String (必填)            // 商家回复内容
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "售后处理成功",
    "data": {
      "service_id": 2001,
      "service_status": "AGREED",
      "seller_decision": "AGREE_REFUND",
      "seller_response": "非常抱歉给您带来不好的体验，已为您同意全额退款，款项将在 1-3 个工作日内原路退回",
      "seller_decision_at": "2026-03-06T16:00:00",
      "updated_at": "2026-03-06T16:00:00"
    }
  }
  ```

  

### 58. 卖家确认收到退货

- **方法**：POST

- **路径**：`/api/seller/after-sales/{service_id}/confirm-return`

- **描述**：在退货退款流程中，卖家确认收到客户退回的商品后操作此接口；系统自动完成退款并更新售后状态为 "COMPLETED"

- **权限**：卖家（需 Authorization 令牌）

- **路径参数**：

  - service_id: 2001 (Number, 必填) - 售后服务单 ID

- **请求体（JSON）**：

  ```json
  {
    "remark": String (可选)                     // 商家备注（如：商品完好无损）
  }
  ```

  

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "退货确认成功，退款已执行",
    "data": {
      "service_id": 2001,
      "service_status": "COMPLETED",
      "return_received_at": "2026-03-08T14:00:00",
      "completed_at": "2026-03-08T14:00:00",
      "refund_executed": true,
      "refund_amount": 299.99,
      "updated_at": "2026-03-08T14:00:00"
    }
  }
  ```

  

### 统一错误响应格式

```json
{ 
  "code": 400, 
  "message": "错误原因说明（如：库存不足、无权删除该地址、售后申请已过期）", 
  "data": null 
}
```



### 59. 查询商品价格历史

- **方法**：GET

- **路径**：`/api/products/{product_id}/price-history`

- **描述**：查询指定商品的价格历史记录，支持时间范围筛选，返回价格趋势数据供前端绘制折线图

- **权限**：公开

- **路径参数**：

  - [product_id](javascript:void(0)): Number (必填) - 商品ID

- **查询参数**：

  ```json
  {
    "days": Number (可选，默认7),           // 查询天数：7/30/90/自定义
    "start_date": String (可选),            // 开始日期（格式：YYYY-MM-DD）
    "end_date": String (可选),              // 结束日期（格式：YYYY-MM-DD）
    "page": Number (可选，默认1),
    "size": Number (可选，默认50)
    }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "product_id": 101,
      "product_name": "无线蓝牙耳机",
      "current_price": 299.99,
      "lowest_price": 249.99,
      "highest_price": 349.99,
      "average_price": 289.50,
      "price_trend": [
        {
          "date": "2026-04-13",
          "price": 299.99,
          "change_type": "MANUAL"
        },
        {
          "date": "2026-04-10",
          "price": 279.99,
          "change_type": "PROMOTION_START"
        },
        {
          "date": "2026-04-05",
          "price": 299.99,
          "change_type": "PROMOTION_END"
        }
      ],
      "statistics": {
        "total_changes": 5,
        "decrease_count": 2,
        "increase_count": 3,
        "max_decrease_percentage": 16.67,
        "max_increase_percentage": 10.00
      },
      "page": 1,
      "size": 50,
      "total": 5
    }
  }
  ```

### 60. 获取商品价格统计摘要

- **方法**：GET

- **路径**：`/api/products/{product_id}/price-summary`

- **描述**：快速获取商品价格统计摘要信息（当前价、最低价、最高价、平均价），用于商品列表页展示

- **权限**：公开

- **路径参数**：

  - [product_id](javascript:void(0)): Number (必填) - 商品ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "product_id": 101,
      "current_price": 299.99,
      "original_price": 349.99,
      "lowest_price_30d": 249.99,
      "highest_price_30d": 349.99,
      "average_price_30d": 289.50,
      "price_change_percentage": -14.29,
      "has_active_promotion": true,
      "promotion_end_time": "2026-04-25T23:59:59"
    }
  }
  ```

### 61. 卖家修改商品价格

- **方法**：PUT

- **路径**：`/api/seller/products/{product_id}/price`

- **描述**：卖家修改商品价格，系统自动记录价格历史并触发价格提醒通知

- **权限**：卖家（需Authorization令牌，仅限自己发布的商品）

- **路径参数**：

  - [product_id](javascript:void(0)): Number (必填) - 商品ID

- **请求体（JSON）**：

  ```json
  {
    "new_price": Number (必填),             // 新价格（必须>0）
    "change_reason": String (可选)          // 价格变更原因/备注
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "价格修改成功",
    "data": {
      "product_id": 101,
      "old_price": 299.99,
      "new_price": 279.99,
      "change_type": "MANUAL",
      "updated_at": "2026-04-19T15:30:00",
      "alerts_triggered": 15                // 触发的价格提醒数量
    }
  }
  ```

### 62. 客户配置价格提醒

- **方法**：POST

- **路径**：`/api/customers/price-alerts`

- **描述**：客户为收藏或购物车中的商品配置价格变动提醒，可设置提醒阈值

- **权限**：登录客户（需Authorization令牌）

- **请求体（JSON）**：

  ```json
  {
    "product_id": Number (必填),            // 商品ID
    "alert_type": String (必填),            // "FAVORITE" 或 "CART"
    "threshold_percentage": Number (可选，默认5.0),  // 价格变动阈值百分比
    "is_enabled": Boolean (可选，默认true)  // 是否启用提醒
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "价格提醒配置成功",
    "data": {
      "alert_id": 3001,
      "product_id": 101,
      "product_name": "无线蓝牙耳机",
      "alert_type": "FAVORITE",
      "threshold_percentage": 5.00,
      "is_enabled": true,
      "current_price": 299.99,
      "created_at": "2026-04-19T16:00:00"
    }
  }
  ```

### 63. 客户查询价格提醒配置列表

- **方法**：GET

- **路径**：`/api/customers/price-alerts`

- **描述**：客户查询自己配置的所有价格提醒，支持按启用状态、商品筛选

- **权限**：登录客户（需Authorization令牌）

- **查询参数**：

  ```json
  {
    "customer_id": Number (必填),
    "is_enabled": Boolean (可选),
    "alert_type": String (可选),            // "FAVORITE" 或 "CART"
    "page": Number (可选，默认1),
    "size": Number (可选，默认10)
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 5,
      "items": [
        {
          "alert_id": 3001,
          "product_id": 101,
          "product_info": {
            "product_name": "无线蓝牙耳机",
            "image_url": "http://localhost:8080/media/product/img1.jpg",
            "current_price": 279.99
          },
          "alert_type": "FAVORITE",
          "threshold_percentage": 5.00,
          "is_enabled": true,
          "last_alerted_price": 299.99,
          "last_alerted_at": "2026-04-18T10:00:00",
          "created_at": "2026-04-15T12:00:00"
        }
      ]
    }
  }
  ```

### 64. 客户更新价格提醒配置

- **方法**：PUT

- **路径**：`/api/customers/price-alerts/{alert_id}`

- **描述**：客户更新价格提醒配置（启用/禁用、修改阈值等）

- **权限**：登录客户（需Authorization令牌，仅限本人配置）

- **路径参数**：

  - `alert_id`: Number (必填) - 提醒配置ID

- **请求体（JSON）**：

  ```json
  {
    "is_enabled": Boolean (可选),
    "threshold_percentage": Number (可选)
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "提醒配置更新成功",
    "data": {
      "alert_id": 3001,
      "is_enabled": false,
      "threshold_percentage": 10.00,
      "updated_at": "2026-04-19T16:30:00"
    }
  }
  ```

### 65. 客户删除价格提醒配置

- **方法**：DELETE

- **路径**：`/api/customers/price-alerts/{alert_id}`

- **描述**：客户删除指定的价格提醒配置

- **权限**：登录客户（需Authorization令牌，仅限本人配置）

- **路径参数**：

  - `alert_id`: Number (必填) - 提醒配置ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "提醒配置删除成功",
    "data": {
      "alert_id": 3001,
      "deleted_at": "2026-04-19T16:45:00"
    }
  }
  ```

### 66. 客户查询价格变动通知列表

- **方法**：GET

- **路径**：`/api/customers/price-alerts/notifications`

- **描述**：客户查询收到的所有价格变动通知，支持按已读/未读筛选

- **权限**：登录客户（需Authorization令牌）

- **查询参数**：

  ```json
  {
    "customer_id": Number (必填),
    "is_read": Boolean (可选),
    "page": Number (可选，默认1),
    "size": Number (可选，默认10)
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 3,
      "unread_count": 2,
      "items": [
        {
          "notification_id": 5001,
          "product_id": 101,
          "product_info": {
            "product_name": "无线蓝牙耳机",
            "image_url": "http://localhost:8080/media/product/img1.jpg"
          },
          "old_price": 299.99,
          "new_price": 279.99,
          "change_percentage": -6.67,
          "change_direction": "DECREASE",
          "is_read": false,
          "created_at": "2026-04-18T10:00:00"
        }
      ]
    }
  }
  ```

### 67. 标记价格通知为已读

- **方法**：PATCH

- **路径**：`/api/customers/price-alerts/notifications/{notification_id}/read`

- **描述**：客户将指定的价格变动通知标记为已读

- **权限**：登录客户（需Authorization令牌，仅限本人通知）

- **路径参数**：

  - `notification_id`: Number (必填) - 通知ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "标记已读成功",
    "data": {
      "notification_id": 5001,
      "is_read": true,
      "read_at": "2026-04-19T17:00:00"
    }
  }
  ```

### 68. 批量标记通知为已读

- **方法**：PATCH

- **路径**：`/api/customers/price-alerts/notifications/read-all`

- **描述**：客户将所有未读的价格变动通知批量标记为已读

- **权限**：登录客户（需Authorization令牌）

- **请求体（JSON）**：

  ```json
  {
    "customer_id": Number (必填)
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "批量标记成功",
    "data": {
      "marked_count": 2,
      "marked_at": "2026-04-19T17:05:00"
    }
  }
  ```

### 69. 创建促销活动

- **方法**：POST

- **路径**：`/api/admin/promotions`

- **描述**：运营人员创建新的促销活动，配置活动类型、时间、规则等

- **权限**：管理员/运营人员（需Authorization令牌）

- **请求体（JSON）**：

  ```json
  {
    "promotion_name": String (必填),        // 活动名称
    "promotion_type": String (必填),        // "DISCOUNT"|"FULL_REDUCTION"|"COUPON"|"FLASH_SALE"
    "description": String (可选),           // 活动描述
    "start_time": String (必填),            // 开始时间（ISO 8601格式）
    "end_time": String (必填),              // 结束时间（ISO 8601格式）
    "discount_value": Number (必填),        // 折扣值（折扣率为0-1之间的小数，如0.8表示8折；满减为具体金额）
    "min_purchase_amount": Number (可选),   // 最低消费金额（满减活动使用）
    "max_discount_amount": Number (可选),   // 最大优惠金额
    "applicable_scope": String (必填),      // "ALL"|"CATEGORY"|"PRODUCT"|"USER_GROUP"
    "target_ids": [Number] (可选),          // 目标ID数组（分类ID/商品ID/用户组ID）
    "priority": Number (可选，默认0),       // 优先级
    "rules": [                              // 可选，附加规则
      {
        "rule_type": String,                // "TIME_RANGE"|"USER_LEVEL"|"QUANTITY_LIMIT"
        "rule_config": Object               // 规则配置JSON
      }
    ]
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "促销活动创建成功",
    "data": {
      "promotion_id": 4001,
      "promotion_name": "春季特惠8折",
      "promotion_type": "DISCOUNT",
      "status": "DRAFT",
      "start_time": "2026-04-20T00:00:00",
      "end_time": "2026-04-25T23:59:59",
      "discount_value": 0.80,
      "applicable_scope": "CATEGORY",
      "target_ids": [1, 2, 3],
      "affected_products_count": 45,
      "created_at": "2026-04-19T18:00:00"
    }
  }
  ```

### 70. 查询促销活动列表

- **方法**：GET

- **路径**：`/api/admin/promotions`

- **描述**：查询所有促销活动，支持按状态、类型、时间筛选

- **权限**：管理员/运营人员（需Authorization令牌）

- **查询参数**：

  ```json
  {
    "status": String (可选),                // "DRAFT"|"ACTIVE"|"ENDED"|"CANCELLED"
    "promotion_type": String (可选),
    "start_date": String (可选),
    "end_date": String (可选),
    "page": Number (可选，默认1),
    "size": Number (可选，默认10)
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "page": 1,
      "size": 10,
      "total": 5,
      "items": [
        {
          "promotion_id": 4001,
          "promotion_name": "春季特惠8折",
          "promotion_type": "DISCOUNT",
          "description": "电子产品类8折优惠",
          "start_time": "2026-04-20T00:00:00",
          "end_time": "2026-04-25T23:59:59",
          "status": "DRAFT",
          "discount_value": 0.80,
          "applicable_scope": "CATEGORY",
          "affected_products_count": 45,
          "created_by": 1,
          "created_at": "2026-04-19T18:00:00"
        }
      ]
    }
  }
  ```

### 71. 查询促销活动详情

- **方法**：GET

- **路径**：`/api/admin/promotions/{promotion_id}`

- **描述**：查询指定促销活动的详细信息，包括关联商品、规则等

- **权限**：管理员/运营人员（需Authorization令牌）

- **路径参数**：

  - `promotion_id`: Number (必填) - 促销活动ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "promotion_id": 4001,
      "promotion_name": "春季特惠8折",
      "promotion_type": "DISCOUNT",
      "description": "电子产品类8折优惠",
      "start_time": "2026-04-20T00:00:00",
      "end_time": "2026-04-25T23:59:59",
      "status": "ACTIVE",
      "discount_value": 0.80,
      "applicable_scope": "CATEGORY",
      "target_ids": [1, 2, 3],
      "priority": 10,
      "rules": [
        {
          "rule_id": 6001,
          "rule_type": "TIME_RANGE",
          "rule_config": {
            "time_ranges": ["10:00-12:00", "20:00-22:00"]
          }
        }
      ],
      "products": [
        {
          "product_id": 101,
          "product_name": "无线蓝牙耳机",
          "original_price": 299.99,
          "promotion_price": 239.99,
          "discount_amount": 60.00
        }
      ],
      "statistics": {
        "total_products": 45,
        "total_orders": 128,
        "total_discount_amount": 15680.00
      },
      "created_at": "2026-04-19T18:00:00",
      "updated_at": "2026-04-20T00:00:00"
    }
  }
  ```

### 72. 更新促销活动

- **方法**：PUT
- **路径**：`/api/admin/promotions/{promotion_id}`
- **描述**：更新促销活动信息（仅草稿状态可编辑）
- **权限**：管理员/运营人员（需Authorization令牌）
- **路径参数**：
  - `promotion_id`: Number (必填) - 促销活动ID
- **请求体（JSON）**：同接口69的请求体
- **响应示例**：同接口69的响应示例

### 73. 激活促销活动

- **方法**：POST

- **路径**：`/api/admin/promotions/{promotion_id}/activate`

- **描述**：激活促销活动，系统自动计算并应用促销价格到相关商品

- **权限**：管理员/运营人员（需Authorization令牌）

- **路径参数**：

  - `promotion_id`: Number (必填) - 促销活动ID

- **请求体（JSON）**：

  ```json
  {
    "confirm": Boolean (必填)               // 确认激活（防止误操作）
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "促销活动激活成功",
    "data": {
      "promotion_id": 4001,
      "status": "ACTIVE",
      "activated_at": "2026-04-19T18:30:00",
      "affected_products_count": 45,
      "estimated_discount_total": 25000.00
    }
  }
  ```

### 74. 结束促销活动

- **方法**：POST

- **路径**：`/api/admin/promotions/{promotion_id}/end`

- **描述**：手动结束促销活动，恢复商品原价

- **权限**：管理员/运营人员（需Authorization令牌）

- **路径参数**：

  - `promotion_id`: Number (必填) - 促销活动ID

- **请求体（JSON）**：

  ```json
  {
    "reason": String (可选)                 // 结束原因
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "促销活动已结束",
    "data": {
      "promotion_id": 4001,
      "status": "ENDED",
      "ended_at": "2026-04-22T10:00:00",
      "actual_duration_days": 2,
      "total_orders": 85,
      "total_discount_amount": 12500.00
    }
  }
  ```

### 75. 取消促销活动

- **方法**：POST

- **路径**：`/api/admin/promotions/{promotion_id}/cancel`

- **描述**：取消促销活动（仅草稿或进行中的活动可取消）

- **权限**：管理员/运营人员（需Authorization令牌）

- **路径参数**：

  - `promotion_id`: Number (必填) - 促销活动ID

- **请求体（JSON）**：

  ```json
  {
    "reason": String (必填)                 // 取消原因
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "促销活动已取消",
    "data": {
      "promotion_id": 4001,
      "status": "CANCELLED",
      "cancelled_at": "2026-04-19T19:00:00"
    }
  }
  ```

### 76. 查询当前活跃的促销活动

- **方法**：GET

- **路径**：`/api/promotions/active`

- **描述**：查询当前正在进行的所有促销活动，供前端展示

- **权限**：公开

- **查询参数**：

  ```json
  {
    "promotion_type": String (可选),
    "product_id": Number (可选)             // 查询指定商品参与的促销
  }
  ```

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": [
      {
        "promotion_id": 4001,
        "promotion_name": "春季特惠8折",
        "promotion_type": "DISCOUNT",
        "discount_value": 0.80,
        "start_time": "2026-04-20T00:00:00",
        "end_time": "2026-04-25T23:59:59",
        "remaining_hours": 144,
        "applicable_scope": "CATEGORY"
      }
    ]
  }
  ```

### 77. 查询商品适用的促销活动

- **方法**：GET

- **路径**：`/api/products/{product_id}/promotions`

- **描述**：查询指定商品当前参与的所有促销活动及最终价格

- **权限**：公开

- **路径参数**：

  - [product_id](javascript:void(0)): Number (必填) - 商品ID

- **响应示例**：

  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "product_id": 101,
      "product_name": "无线蓝牙耳机",
      "original_price": 299.99,
      "current_price": 239.99,
      "has_active_promotion": true,
      "promotions": [
        {
          "promotion_id": 4001,
          "promotion_name": "春季特惠8折",
          "promotion_type": "DISCOUNT",
          "discount_value": 0.80,
          "discount_amount": 60.00,
          "final_price": 239.99,
          "start_time": "2026-04-20T00:00:00",
          "end_time": "2026-04-25T23:59:59",
          "priority": 10
        }
      ],
      "best_promotion": {
        "promotion_id": 4001,
        "final_price": 239.99,
        "save_amount": 60.00
      }
    }
  }
  ```

### 78. 系统定时任务：检查并激活/结束促销活动

- **方法**：无（系统内部定时任务，建议每分钟执行一次）
- **路径**：无
- **描述**：系统定时检查促销活动时间，自动激活到期的活动和结束过期的活动
- **权限**：系统自动执行
- **触发逻辑**：
  1. 查询 `promotions` 表中 `status='DRAFT'` 且 `start_time <= 当前时间` 的记录，批量更新为 `status='ACTIVE'`
  2. 查询 `promotions` 表中 `status='ACTIVE'` 且 `end_time < 当前时间` 的记录，批量更新为 `status='ENDED'`
  3. 对于激活的活动，计算并更新 `product_promotions` 表中的促销价格
  4. 对于结束的活动，恢复商品原价，清除 `product_promotions` 中的关联
  5. 记录执行日志
- **日志记录**：每次执行后记录激活/结束的活动数量到系统日志

### 79. 系统定时任务：检查价格变动并发送通知

- **方法**：无（系统内部定时任务，建议每5分钟执行一次）
- **路径**：无
- **描述**：系统定时检查商品价格变动，对比提醒配置中的阈值，向符合条件的用户发送通知
- **权限**：系统自动执行
- **触发逻辑**：
  1. 查询 `price_alert_settings` 表中 `is_enabled=true` 的配置
  2. 获取对应商品的当前价格
  3. 计算价格变动百分比，判断是否超过阈值
  4. 如果超过阈值且距离上次提醒超过一定时间（如1小时），创建通知记录
  5. 更新 `last_alerted_price` 和 `last_alerted_at`
  6. 可通过站内信、邮件等方式通知用户
- **日志记录**：每次执行后记录发送的通知数量到系统日志
