<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.onlineshop.mapper.LogisticsProviderMapper">

    <resultMap id="LogisticsProviderMap" type="com.example.onlineshop.entity.LogisticsProvider">
        <id property="providerId" column="provider_id"/>
        <result property="providerName" column="provider_name"/>
        <result property="providerCode" column="provider_code"/>
        <result property="enabled" column="enabled"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="LogisticsProviderMap">
        SELECT * FROM logistics_providers
        WHERE enabled = TRUE
        ORDER BY sort_order ASC
    </select>

    <select id="findByType" resultMap="LogisticsProviderMap">
        SELECT * FROM logistics_providers
        WHERE enabled = TRUE
        <if test="type != null and type == 'EXPRESS'">
            AND provider_code != 'OFFLINE'
        </if>
        <if test="type != null and type == 'OFFLINE'">
            AND provider_code = 'OFFLINE'
        </if>
        ORDER BY sort_order ASC
    </select>

    <select id="findById" parameterType="int" resultMap="LogisticsProviderMap">
        SELECT * FROM logistics_providers WHERE provider_id = #{providerId}
    </select>

    <select id="findByCode" parameterType="string" resultMap="LogisticsProviderMap">
        SELECT * FROM logistics_providers WHERE provider_code = #{providerCode}
    </select>

</mapper>