<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shop.mapper.SellerMapper">

    <!-- 按用户名查询卖家 -->
    <select id="selectByUsername" parameterType="String" resultType="com.example.shop.entity.Seller">
        SELECT seller_id, username, password, create_time
        FROM seller
        WHERE username = #{username}
    </select>

    <!-- 按ID查询卖家 -->
    <select id="selectById" parameterType="Integer" resultType="com.example.shop.entity.Seller">
        SELECT seller_id, username, password, create_time
        FROM seller
        WHERE seller_id = #{sellerId}
    </select>

    <!-- 更新密码 -->
    <update id="updatePassword">
        UPDATE seller
        SET password = #{newPassword}
        WHERE seller_id = #{sellerId}
    </update>

</mapper>