<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.onlineshop.mapper.ProductPriceHistoryMapper">

    <resultMap id="BaseResultMap" type="com.example.onlineshop.entity.ProductPriceHistory">
        <id column="history_id" property="historyId"/>
        <result column="product_id" property="productId"/>
        <result column="old_price" property="oldPrice"/>
        <result column="new_price" property="newPrice"/>
        <result column="change_type" property="changeType"/>
        <result column="change_reason" property="changeReason"/>
        <result column="changed_by" property="changedBy"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="historyId">
        INSERT INTO product_price_history (product_id, old_price, new_price, change_type, change_reason, changed_by)
        VALUES (#{productId}, #{oldPrice}, #{newPrice}, #{changeType}, #{changeReason}, #{changedBy})
    </insert>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT * FROM product_price_history
        WHERE product_id = #{productId}
        <if test="startDate != null">
            AND created_at &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND created_at &lt;= #{endDate}
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByProductId" resultType="int">
        SELECT COUNT(*) FROM product_price_history
        WHERE product_id = #{productId}
        <if test="startDate != null">
            AND created_at &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND created_at &lt;= #{endDate}
        </if>
    </select>

    <select id="selectLatestByProductId" resultMap="BaseResultMap">
        SELECT * FROM product_price_history
        WHERE product_id = #{productId}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

</mapper>